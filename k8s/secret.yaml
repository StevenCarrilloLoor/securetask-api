# Secreto de ejemplo. En producción se inyecta desde un gestor de secretos
# (Sealed Secrets, Vault, SOPS) — NUNCA se versiona el valor real.
apiVersion: v1
kind: Secret
metadata:
  name: securetask-secrets
  namespace: securetask
type: Opaque
stringData:
  SECRET_KEY: "ci-demo-secret-please-change-in-prod"
